language: python
python:
  - "3.6"
  - "3.6-dev"
before_install:
  - "sudo apt-get -qq update"
  - "sudo apt-get -y install libsdl1.2-dev"
  - "pip install -U pip"
install:
  - "pip install -r requirements/tests.txt"
  - "pip install ."
script: py.test
